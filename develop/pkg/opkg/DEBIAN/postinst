#!/bin/sh

PATH=/sbin:/bin:/usr/sbin:/usr/bin

case "$1" in
  configure)
    sed -i 's#:latest#:develop#g' /etc/systemd/system/flecs-*
    sed -i 's#:latest#:develop#g' /usr/bin/flecs-updater
    ;;
  abort-upgrade|abort-remove|abort-deconfigure)
    ;;
  *)
    ;;
esac

exit 0
